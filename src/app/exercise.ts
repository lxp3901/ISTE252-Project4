// defines the exercise object
export class Exercise {
    id: number; // the id of the exercise
    name: string; // the name of the exercise

    constructor(id: number, name: string) {
        this.id = id;
        this.name = name;
    }
  }